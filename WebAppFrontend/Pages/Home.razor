@page "/"
@inject NavigationManager Nav

<div class="d-flex justify-content-center align-items-center vh-100">
    <div class="card shadow-lg p-5 text-center" style="width: 28rem; border-radius: 1rem;">

        <h1 class="mb-4">🚢 Battleship</h1>

        <p class="text-muted mb-4">
            Choose your game mode
        </p>

        <button class="btn btn-primary w-100 py-2 mb-3" style="font-size: 1.1rem;" @onclick="StartLocalGame">
            🤖 Local Game (vs AI)
        </button>

        <button class="btn btn-success w-100 py-2" style="font-size: 1.1rem;" @onclick="StartMultiplayerGame">
            🌐 Multiplayer
        </button>
    </div>
</div>

@code {
    void StartLocalGame()
    {
        Nav.NavigateTo("/placement");
    }

    void StartMultiplayerGame()
    {
        Nav.NavigateTo("/multiplayer");
    }
}